/*
Example S3 backend configuration. Enable by filling the values and removing the comment block.

terraform {
  backend "s3" {
    # bucket         = "my-terraform-state-bucket"
    # key            = "terraform/infra/terraform.tfstate"
    # region         = "us-east-1"
    # dynamodb_table = "terraform-locks"
    # encrypt        = true
  }
}
*/
